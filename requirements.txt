# LabBREW – Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# The parameterDB sub-package (Services/parameterDB/) also has its own
# pyproject.toml for editable / wheel installs; the constraints from that
# file are reflected here.

# Web framework & server
fastapi
uvicorn
pydantic

# HTTP client
requests

# Binary serialisation (parameterDB protocol)
msgpack>=1.0

# Excel schedule import
openpyxl

# CAN-bus hardware interface (BrewTools Kvaser source)
python-can

# Serial hardware interface (labps3005dn / modbus-relay sources)
pyserial>=3.5

# YAML topology & data files
pyyaml

# mDNS service discovery
zeroconf
